{
  "name": "xp_scaling",
  "version": "1.0.0",
  "description": "Create a docker-compose.yml and a main.kevs to test Kevoree JS scaling using Docker",
  "main": "create-script.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^3.7.0",
    "kevoree-const": "^1.0.1"
  },
  "devDependencies": {
    "eslint": "^3.12.0"
  },
	"eslintConfig": {
		"parserOptions": {
			"ecmaVersion": 6
		},
		"env": {
			"node": true
		},
		"extends": ["eslint:recommended"],
		"rules": {
			"no-console": 0
		}
	}
}
